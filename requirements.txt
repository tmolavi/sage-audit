# ---------------------------------------------------------------------------
# sage-audit — core runtime dependencies
# The Unified 3-Pillar Audit Engine for SEO, Entity AEO, and GEO.
# (c) 2026 Taqi Molavi — https://molavi.pro — MIT License
# ---------------------------------------------------------------------------
rich>=13.0.0
click>=8.1.0
beautifulsoup4>=4.12.0
trafilatura>=1.6.0
pydantic>=2.5.0
mcp>=1.2.0
requests>=2.31.0

# ---------------------------------------------------------------------------
# Optional (recommended) neural embedding backends for the GEO pillar.
# If neither is installed, sage-audit automatically falls back to a
# deterministic hashed n-gram TF vectorizer and never crashes.
# ---------------------------------------------------------------------------
# fastembed>=0.2.6
# sentence-transformers>=2.2.0

# ---------------------------------------------------------------------------
# Development / testing
# ---------------------------------------------------------------------------
# pytest>=7.4.0
# pytest-cov>=4.1.0
# ruff>=0.4.0
# mypy>=1.8.0
